<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Figure</title>
  </head>
  <body>
    <form action="/figures" method="post">
      <p>Name: <input type="text" name="figure[name]" id="figure_name" value=""></p>

      <h3>Titles</h3> <br>
      <% @titles.each do |title|%>
        <input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>" value="<%= title.id %>"><%= title.name %> <br>
      <% end %>

      <h3>Create New Title:</h3>
      <p>Name: <input type="text" name="title[name]" id="new_title"></p>

      <h3>Landmarks</h3>
      <% @landmarks.each do |landmark| %>
        <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%= landmark.id %>" value="<%= landmark.id %>"><%= landmark.name %> <br>
      <% end %>

      <h3>Create New Landmark:</h3>
      <p>Name: <input type="text" name="landmark[name]" id="new_landmark"></p>
      <p>Year: <input type="text" name="landmark[year]" id="landmark_year"></p>

      <br>

      <input type="submit" name="" value="Create New Figure">
    </form>
  </body>
</html>
